import { Fragment } from "react";

const KerangaPage = (props) => {
  document.title = "Раздатка КиЭ 35 | Кампания из Эвенглена";

  return (
    <Fragment>
    </Fragment>
  );
};

export default KerangaPage;
